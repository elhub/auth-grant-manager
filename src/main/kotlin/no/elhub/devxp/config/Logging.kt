package no.elhub.devxp.config

import io.ktor.server.application.Application

fun Application.configureLogging() {
    // slf4jLogger() is a function that configures the Koin logger to use the SLF4J logger.
    // Logback is a logging framework that is a successor to the popular log4j project.
    // LogFMT is a structured logging library that allows you to log messages in a structured way.
}
