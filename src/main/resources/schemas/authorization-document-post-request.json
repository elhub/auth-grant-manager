{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://api.elhub.no/schemas/authorization-request.json",
  "title": "Authorization Request Request",
  "description": "Schema for creating authorization documents to the Elhub authorization manager.",
  "type": "object",
  "properties": {
    "data": {
      "description": "The authorization document to be created.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "description": "The type of the resource object.",
          "const": "AuthorizationDocument"
        },
        "attributes": {
          "type": "object",
          "description": "The attributes of the authorization document.",
          "properties": {
            "documentType": {
              "type": "string",
              "description": "The type of the authorization document.",
              "enum": ["ChangeOfSupplierConfirmation"]
            },
            "meteringPointId": {
              "type": "string",
              "description": "The unique identifier of the metering point associated with the authorization document."
            },
            "address": {
              "type": "string",
              "description": "The address of the metering point."
            }
          }
        },
        "relationships": {
          "type": "object",
          "description": "The relationships of the authorization document.",
          "properties": {
            "requestedFrom": {
              "$ref": "base-definitions.schema.json#/definitions/relationshipParty"
            },
            "requestedTo": {
              "$ref": "base-definitions.schema.json#/definitions/relationshipParty"
            }
          }
        }
      }
    }
  },
  "required": ["data"]
}
