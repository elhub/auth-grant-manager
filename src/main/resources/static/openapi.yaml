---
openapi: 3.1.1
info:
  title: Authorization Grant Manager
  version: 0.1.0
  description: >
    The authorization grant manager handles the authorization grants for Elhub's APIs. Note that we are still iterating
    over the authorization-grant APIs. Endpoints marked TODO are under active development and not yet enabled.
  x-status: Development
  contact:
    name: team-devxp
  license:
    name: MIT
    url: https://github.com/elhub/auth-grant-manager?tab=MIT-1-ov-file
paths:
  "/authorization-documents":
    get:
      summary: Retrieve a list of authorization document objects.
      parameters:
        - $ref: "#/components/parameters/UserAgent"
        - name: Authorization
          in: header
          required: true
          description: Bearer Maskinporten token for the calling party.
          schema:
            type: string
        - name: SenderGln
          in: header
          required: true
          description: GLN of the party issuing the request.
          schema:
            type: string
        - name: OnBehalfOfGln
          in: header
          required: false
          description: GLN of the party the sender is acting on behalf of in delegated Maskinporten flows.
          schema:
            type: string
      responses:
        "200":
          description: A list of authorization document objects.
          content:
            application/json:
              schema:
                $ref: "./schemas/documents/authorization-document-get-list-response.schema.json"
        "401":
          $ref: "#/components/responses/401UnauthorizedError"
        "500":
          $ref: "#/components/responses/500InternalServerError"
    post:
      summary: Initiate a new authorization document flow.
      description: Initiate a new authorization document flow. This creates a authorization document for signing.
      parameters:
        - $ref: "#/components/parameters/UserAgent"
        - name: Authorization
          in: header
          required: true
          description: Bearer Maskinporten token for the calling party .
          schema:
            type: string
        - name: SenderGln
          in: header
          required: true
          description: GLN of the party issuing the request.
          schema:
            type: string
        - name: OnBehalfOfGln
          in: header
          required: false
          description: GLN of the party the sender is acting on behalf of in delegated Maskinporten flows.
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "./schemas/documents/authorization-document-post-request.schema.json"
      responses:
        "201":
          description: Returns the identifier of the created authorization document together with links to the resource and its PDF file.
          content:
            application/json:
              schema:
                $ref: "./schemas/documents/authorization-document-post-response.schema.json"
        "400":
          $ref: "#/components/responses/400BadRequestError"
        "401":
          $ref: "#/components/responses/401UnauthorizedError"
        "500":
          $ref: "#/components/responses/500InternalServerError"
  "/authorization-documents/{id}":
    get:
      summary: Retrieve information about a specific authorization document.
      description: |
        Retrieve information about a specific authorization document. This can be used to check the status as well as other attributes of the document.
      parameters:
        - $ref: "#/components/parameters/UserAgent"
        - name: Authorization
          in: header
          required: true
          description: Bearer Maskinporten token for the calling party.
          schema:
            type: string
        - name: SenderGln
          in: header
          required: true
          description: GLN of the party issuing the request.
          schema:
            type: string
        - name: OnBehalfOfGln
          in: header
          required: false
          description: GLN of the party the sender is acting on behalf of in delegated Maskinporten flows.
          schema:
            type: string
        - name: id
          in: path
          required: true
          description: The id of the authorization document to retrieve.
          schema:
            type: string
      responses:
        "200":
          description: This returns information about the authorization document.
          content:
            application/json:
              schema:
                $ref: "./schemas/documents/authorization-document-get-response.schema.json"
        "401":
          $ref: "#/components/responses/401UnauthorizedError"
        "404":
          $ref: "#/components/responses/404NotFoundError"
        "500":
          $ref: "#/components/responses/500InternalServerError"
    delete:
      summary: Delete/cancel a signed document.
      description: Cancel a document to be signed. This can only be done if the document is not yet signed.
      parameters:
        - $ref: "#/components/parameters/UserAgent"
        - name: Authorization
          in: header
          required: true
          description: Bearer Maskinporten token for the calling party.
          schema:
            type: string
        - name: SenderGln
          in: header
          required: true
          description: GLN of the party issuing the request.
          schema:
            type: string
        - name: OnBehalfOfGln
          in: header
          required: false
          description: GLN of the party the sender is acting on behalf of in delegated Maskinporten flows.
          schema:
            type: string
        - name: id
          in: path
          required: true
          description: The id of the authorization document to delete.
          schema:
            type: string
      responses:
        "200":
          $ref: "#/components/responses/MetaResponse"
        "401":
          $ref: "#/components/responses/401UnauthorizedError"
        "403":
          $ref: "#/components/responses/403ForbiddenError"
        "404":
          $ref: "#/components/responses/404NotFoundError"
        "500":
          $ref: "#/components/responses/500InternalServerError"
      tags:
        - TODO
  "/authorization-documents/{id}.pdf":
    get:
      summary: Retrieve authorization document.
      description: Retrieve the actual authorization document as a PDF file.
      parameters:
        - $ref: "#/components/parameters/UserAgent"
        - name: Authorization
          in: header
          required: true
          description: Bearer Maskinporten token for the calling party.
          schema:
            type: string
        - name: SenderGln
          in: header
          required: true
          description: GLN of the party issuing the request.
          schema:
            type: string
        - name: OnBehalfOfGln
          in: header
          required: false
          description: GLN of the party the sender is acting on behalf of in delegated Maskinporten flows.
          schema:
            type: string
        - name: id
          in: path
          required: true
          description: The id of the authorization document to retrieve.
          schema:
            type: string
      responses:
        "200":
          description: This returns information about the authorization document.
          content:
            application/pdf:
              schema:
                type: string
        "401":
          $ref: "#/components/responses/401UnauthorizedError"
        "404":
          $ref: "#/components/responses/404NotFoundError"
        "500":
          $ref: "#/components/responses/500InternalServerError"
    put:
      summary: Submit a signed authorization document.
      description: Submit a signed authorization document. Elhub will validate the document, generate any necessary authorization grants and store the document.
      parameters:
        - $ref: "#/components/parameters/UserAgent"
        - name: Authorization
          in: header
          required: true
          description: Bearer Maskinporten token for the calling party.
          schema:
            type: string
        - name: SenderGln
          in: header
          required: true
          description: GLN of the party issuing the request.
          schema:
            type: string
        - name: OnBehalfOfGln
          in: header
          required: false
          description: GLN of the party the sender is acting on behalf of in delegated Maskinporten flows.
          schema:
            type: string
        - name: id
          in: path
          required: true
          description: The id of the authorization document to change.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/pdf:
            schema:
              type: string
              format: binary
      responses:
        "204":
          description: The signature was validated and the document was registered and accepted. No content is returned.
        "400":
          $ref: "#/components/responses/400BadRequestError"
        "401":
          $ref: "#/components/responses/401UnauthorizedError"
        "500":
          $ref: "#/components/responses/500InternalServerError"
  "/authorization-grants":
    get:
      summary: Retrieve a list of authorization grants.
      parameters:
        - $ref: "#/components/parameters/UserAgent"
        - name: Authorization
          in: header
          required: true
          description: Bearer Maskinporten token or Enduser token.
          schema:
            type: string
        - name: SenderGln
          in: header
          required: false
          description: GLN of the party issuing the request.
          schema:
            type: string
        - name: OnBehalfOfGln
          in: header
          required: false
          description: GLN of the party the sender is acting on behalf of in delegated Maskinporten flows.
          schema:
            type: string
      responses:
        "200":
          description: A list of authorization grants.
          content:
            application/json:
              schema:
                $ref: "./schemas/grants/authorization-grant-get-list-response.schema.json"
        "400":
          $ref: "#/components/responses/400BadRequestError"
        "401":
          $ref: "#/components/responses/401UnauthorizedError"
        "500":
          $ref: "#/components/responses/500InternalServerError"
  "/authorization-grants/{id}":
    get:
      summary: Retrieve a specific authorization grant.
      parameters:
        - $ref: "#/components/parameters/UserAgent"
        - name: Authorization
          in: header
          required: false
          description: Bearer Maskinporten token or Enduser token.
          schema:
            type: string
        - name: SenderGln
          in: header
          required: false
          description: GLN of the party issuing the request.
          schema:
            type: string
        - name: OnBehalfOfGln
          in: header
          required: false
          description: GLN of the party the sender is acting on behalf of in delegated Maskinporten flows.
          schema:
            type: string
        - name: id
          in: path
          required: true
          description: The id of the authorization grant to retrieve.
          schema:
            type: string
      responses:
        "200":
          description: The authorization grant.
          content:
            application/json:
              schema:
                $ref: "./schemas/grants/authorization-grant-get-response.schema.json"
        "401":
          $ref: "#/components/responses/401UnauthorizedError"
        "404":
          $ref: "#/components/responses/404NotFoundError"
        "400":
          $ref: "#/components/responses/400BadRequestError"
        "500":
          $ref: "#/components/responses/500InternalServerError"
    patch:
      summary: Update the status of an authorization grant
      description: Update the authorization grant status. This is an internal API for BRS.
      parameters:
        - $ref: "#/components/parameters/UserAgent"
        - name: id
          in: path
          required: true
          description: The id of the authorization grant to update.
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "./schemas/grants/authorization-grant-patch-request.schema.json"
      responses:
        "200":
          description: The authorization grant was updated.
          content:
            application/json:
              schema:
                $ref: "./schemas/grants/authorization-grant-get-response.schema.json"
        "400":
          $ref: "#/components/responses/400BadRequestError"
        "401":
          $ref: "#/components/responses/401UnauthorizedError"
        "404":
          $ref: "#/components/responses/404NotFoundError"
        "500":
          $ref: "#/components/responses/500InternalServerError"
      tags:
        - TODO
  "/authorization-grants/{id}/scopes":
    get:
      summary: Retrieve all of the scopes for the identified authorization grant.
      parameters:
        - $ref: "#/components/parameters/UserAgent"
        - name: Authorization
          in: header
          required: true
          description: Bearer Maskinporten token or Enduser token.
          schema:
            type: string
        - name: SenderGln
          in: header
          required: false
          description: GLN of the party issuing the request.
          schema:
            type: string
        - name: OnBehalfOfGln
          in: header
          required: false
          description: GLN of the party the sender is acting on behalf of in delegated Maskinporten flows.
          schema:
            type: string
        - name: id
          in: path
          required: true
          description: The id of the authorization grant to retrieve.
          schema:
            type: string
      responses:
        "200":
          description: Returns a list of authorization scopes.
          content:
            application/json:
              schema:
                $ref: "./schemas/grants/authorization-scope-get-list-response.schema.json"
        "400":
          $ref: "#/components/responses/400BadRequestError"
        "401":
          $ref: "#/components/responses/401UnauthorizedError"
        "404":
          $ref: "#/components/responses/404NotFoundError"
        "500":
          $ref: "#/components/responses/500InternalServerError"
  "/authorization-requests":
    get:
      summary: Retrieve a list of authorization requests.
      parameters:
        - $ref: "#/components/parameters/UserAgent"
        - name: Authorization
          in: header
          required: true
          description: Bearer Maskinporten token or Enduser token.
          schema:
            type: string
        - name: SenderGln
          in: header
          required: false
          description: GLN of the party issuing the request.
          schema:
            type: string
        - name: OnBehalfOfGln
          in: header
          required: false
          description: GLN of the party the sender is acting on behalf of in delegated Maskinporten flows.
          schema:
            type: string
      responses:
        "200":
          description: A list of authorization requests.
          content:
            application/json:
              schema:
                $ref: "./schemas/requests/authorization-request-get-list-response.schema.json"
        "401":
          $ref: "#/components/responses/401UnauthorizedError"
        "500":
          $ref: "#/components/responses/500InternalServerError"
      tags:
        - TODO
    post:
      summary: Initiate a new authorization request flow.
      description: Initiate a new authorization request flow. This creates an authorization request that is associated to a user in MinSide.
      parameters:
        - $ref: "#/components/parameters/UserAgent"
        - name: Authorization
          in: header
          required: true
          description: Bearer Maskinporten token for the calling party.
          schema:
            type: string
        - name: SenderGln
          in: header
          required: true
          description: GLN of the party issuing the request.
          schema:
            type: string
        - name: OnBehalfOfGln
          in: header
          required: false
          description: GLN of the party the sender is acting on behalf of in delegated Maskinporten flows.
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "./schemas/requests/authorization-request-post-request.schema.json"
      responses:
        "201":
          description: |
            This creates a new authorization request and returns the details of the request initiated (including the link to the request flow on MinSide).
          content:
            application/json:
              schema:
                $ref: "./schemas/requests/authorization-request-post-response.schema.json"
        "400":
          $ref: "#/components/responses/400BadRequestError"
        "401":
          $ref: "#/components/responses/401UnauthorizedError"
        "500":
          $ref: "#/components/responses/500InternalServerError"
      tags:
        - TODO
  "/authorization-requests/{id}":
    get:
      summary: Retrieve information about a specific authorization request.
      description: Retrieve information about a specific authorization request. This can be used to check the status.
      parameters:
        - $ref: "#/components/parameters/UserAgent"
        - name: Authorization
          in: header
          required: true
          description: Bearer Maskinporten token or Enduser token.
          schema:
            type: string
        - name: SenderGln
          in: header
          required: false
          description: GLN of the party issuing the request.
          schema:
            type: string
        - name: OnBehalfOfGln
          in: header
          required: false
          description: GLN of the party the sender is acting on behalf of in delegated Maskinporten flows.
          schema:
            type: string
        - name: id
          in: path
          required: true
          description: The id of the authorization request to retrieve.
          schema:
            type: string
      responses:
        "200":
          description: This returns information about the authorization request.
          content:
            application/json:
              schema:
                $ref: "./schemas/requests/authorization-request-get-response.schema.json"
        "401":
          $ref: "#/components/responses/401UnauthorizedError"
        "404":
          $ref: "#/components/responses/404NotFoundError"
        "500":
          $ref: "#/components/responses/500InternalServerError"
      tags:
        - TODO
    patch:
      summary: Update an authorization request.
      description: Update an authorization request. This is an internal API for MinSide.
      parameters:
        - $ref: "#/components/parameters/UserAgent"
        - name: Authorization
          in: header
          required: true
          description: Enduser token.
          schema:
            type: string
        - name: id
          in: path
          required: true
          description: The id of the authorization request to update.
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "./schemas/requests/authorization-request-patch-request.schema.json"
      responses:
        "200":
          description: The authorization request was updated.
          content:
            application/json:
              schema:
                $ref: "./schemas/requests/authorization-request-get-response.schema.json"
        "400":
          $ref: "#/components/responses/400BadRequestError"
        "401":
          $ref: "#/components/responses/401UnauthorizedError"
        "404":
          $ref: "#/components/responses/404NotFoundError"
        "500":
          $ref: "#/components/responses/500InternalServerError"
      tags:
        - TODO
    delete:
      summary: Delete/cancel an authorization request.
      description: Cancel an authorization request. This can only be done if the request is not yet authorized.
      parameters:
        - $ref: "#/components/parameters/UserAgent"
        - name: Authorization
          in: header
          required: true
          description: Bearer Maskinporten token for the calling party.
          schema:
            type: string
        - name: SenderGln
          in: header
          required: true
          description: GLN of the party issuing the request
          schema:
            type: string
        - name: OnBehalfOfGln
          in: header
          required: false
          description: GLN of the party the sender is acting on behalf of in delegated Maskinporten flows.
          schema:
            type: string
        - name: id
          in: path
          required: true
          description: The id of the authorization request to delete.
          schema:
            type: string
      responses:
        "200":
          $ref: "#/components/responses/MetaResponse"
        "401":
          $ref: "#/components/responses/401UnauthorizedError"
        "403":
          $ref: "#/components/responses/403ForbiddenError"
        "404":
          $ref: "#/components/responses/404NotFoundError"
        "500":
          $ref: "#/components/responses/500InternalServerError"
      tags:
        - TODO
components:
  parameters:
    UserAgent:
      name: User-Agent
      in: header
      required: true
      description: Identifies the client software originating the request.
      schema:
        type: string
  responses:
    400BadRequestError:
      description: Bad request. Invalid request body.
      content:
        application/json:
          schema:
            $ref: "./schemas/json-api-error.schema.json"
    401UnauthorizedError:
      description: Unauthorized.
      content:
        application/json:
          schema:
            $ref: "./schemas/json-api-error.schema.json"
    403ForbiddenError:
      description: Forbidden.
      content:
        application/json:
          schema:
            $ref: "./schemas/json-api-error.schema.json"
    404NotFoundError:
      description: Not found.
      content:
        application/json:
          schema:
            $ref: "./schemas/json-api-error.schema.json"
    500InternalServerError:
      description: Internal server error.
      content:
        application/json:
          schema:
            $ref: "./schemas/json-api-error.schema.json"
    MetaResponse:
      description: Metadata response for informational purposes.
      content:
        application/json:
          schema:
            $ref: "./schemas/json-api-meta.schema.json"
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
